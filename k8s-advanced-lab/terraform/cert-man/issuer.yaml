apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: issuer
spec:
  acme:
    server: "https://acme-v02.api.letsencrypt.org/directory"
    email: "sayedsylvainltd@gmail.com"
    privateKeySecretRef:
      name: issuer
    solvers:
    - dns01:
        route53:
          hostedZoneID: Z02442932LWV6H10ZIAK6
          region: us-east-1
      selector:
        dnsZones:
        - "lab.mohammedsayed.com"
    - http01:
        ingress:
          class: "nginx"
